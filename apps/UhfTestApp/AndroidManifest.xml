<?xml version="1.0" encoding="utf-8"?>
<!-- UHF-SRS-001 platform test app. uses-feature required="false" so it still installs and shows
     a clear "no reader" status on a device/build without the feature, per UhfManager's own
     never-throw-on-isReaderAvailable() contract. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rockchip.uhf.testapp">

    <uses-feature android:name="android.hardware.uhf" android:required="false" />

    <uses-permission android:name="android.permission.UHF_READ" />
    <uses-permission android:name="android.permission.UHF_WRITE" />

    <application
        android:label="UHF Test"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
